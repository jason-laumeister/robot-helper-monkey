#!/usr/bin/env bash
set -e # halt script on error

echo 'Force commit and push to rebuild github pages by updating the hash...'

echo 'DISABLED - commented out code below to prevent accidental pushes'

# git commit -m 'force rebuild of github pages' --allow-empty
# git push origin master
